# Feature engineering

| File | Description |
|---|---|
| `notebooks/build_features.py` | Build the card/client entity feature tables and on-demand feature functions in the Unity Catalog Feature Store (written to the per-user `fraud` ML schema) for training and real-time scoring. |
