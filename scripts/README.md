# Scripts

Helper scripts run from a terminal (not part of the bundle).

| File | Description |
|---|---|
| `generate_src.py` | Regenerate the gapped `src/` tree from the complete source in `solution/` by stripping solution blocks. |
